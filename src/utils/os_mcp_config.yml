# OpenSearch MCP Server Configuration
# For the remote-agent-server fallback agent, enable all tools.
# Sub-agents can use their own filtered configs if needed.
tool_filters:
  enabled_tools: []
  disabled_tools: []
